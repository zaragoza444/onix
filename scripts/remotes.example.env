# Copy to remotes.env (gitignored) and fill in, then:
#   Get-Content scripts/remotes.env | ForEach-Object { if ($_ -match '^([^#][^=]+)=(.*)$') { Set-Item "env:$($matches[1])" $matches[2] } }
#   powershell -File scripts/publish-remotes.ps1

GITHUB_TOKEN=
GITEA_TOKEN=
